// lib/palette.ts — color tokens for chart accents that don't fit the
// shadcn --chart-1..--chart-12 system.

// Vibrant accent reserved for the AI band (jobbmarked segment 2's bottom
// band, oppstart segment 2's bottom band) — distinct from the rotating
// chart-* tokens so the AI overlay reads as a separate visual layer.
export const AI_COLOR = "oklch(0.62 0.22 250)";
