// Vitest stub for `server-only` — Next.js' sentinel module that errors
// at build time if imported from a client component. Test code has no
// client/server boundary, so the safe behavior is a no-op.
export {};
