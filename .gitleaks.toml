title = "kibarometer gitleaks"

[extend]
useDefault = true

[allowlist]
paths = [
  '''.env.example''',
  # Vendored from upstream supabase/docker. Contains RFC 6455 WebSocket
  # protocol example nonces (base64) that gitleaks flags as generic-api-key.
  '''docker/supabase/tests/.*''',
]
